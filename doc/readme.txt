For configuring Flask:
1) install flask (pip install Flask)
2) install flask-cors (pip install -U flask-cors)
3) launch app.py (python app.py)

For configuiring the baseterm_finder:
1) install gensim (pip install --upgrade gensim)
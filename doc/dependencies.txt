For Flask configuration:
- install flask (pip install Flask)
- install flask-cors (pip install -U flask-cors)

Dependencies:
- spacy (pip install -U spacy)
- nltk (pip install -U nltk)

Launch command:
- launch app.py (python app.py)